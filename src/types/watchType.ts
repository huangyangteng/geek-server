export interface WatchItemContent {
    id:String,
    type:String,
    list:[]
}

export interface WatchChildItem{
    id:String,
    title:String,
    list:String[]
}
export interface WatchBBItem{
    id:Number,
    type:String,
}